<!-- 0018-r1.md -->
# Assignment 0018 review (batch protocol v5 / lifecycle v4)

- Batch: `post-0013-hardening-2026-09-23`; control SHA: `5cb565cc66bdd940a620a9c9eca4dd8acfcae66e`.
- Seed: `b1a3ed84b64ec0c8c9f7e75d175d87cba4c37c84`; base/previous batch merge: `db89e4931de189de802aff9dfaf1189a4788d1f9`.
- Implementation: explicit `rehearse_restore` and storage CLI/Just entry points migrate a disposable fake active database, call the real `restore_backup` path with the supplied candidate, revalidate the restored target physically, and run the application doctor. JSON distinguishes restore mechanics, physical integrity/FK/schema and doctor health; only all-pass exits zero. A temporary directory owns fake active, sidecars, replacement and safety backup.
- Scope: storage operation/CLI/Just recipe, focused WAL/corruption/doctor/cleanup tests, installed-wheel CLI smoke, factual docs. No active restore behavior, schema, provider, dependency/runtime/workflow or Stage 26 product changes.
- Self-review: `origin/main...HEAD` scope matches the issued operation. The configured active database path is only resolved for candidate identity rejection; no active connection, checkpoint or replacement occurs. An empty packaged-migrated and doctor-healthy database is the allowed equivalent fake active. Real restore machinery handles candidate snapshot/WAL semantics. Temporary files are removed on success and failure.
- Focused verification: 45 storage/doctor/rehearsal/installed-wheel tests passed. Cases cover healthy candidate, SQLite-invalid candidate, wrong Alembic revision, physically valid but doctor-unhealthy normalized data, WAL-only committed candidate row, active file/WAL byte preservation, CLI exit/JSON and cleanup after failure.
- Canonical verification: `just check` (317 passed), `just migration-check`, `just corpus-check` (42), `just scenario-check` (42), `just scenario-eval` (42/42), and `just provider-contract` (23 passed) passed; scenario artifact is in `/tmp`.
- Pre-PR corrections: none after test execution. Post-CI corrections: four test-only stabilization passes for SQLite WAL byte-snapshot assertions, including the existing bootstrap preflight assertion that blocked Python 3.13 CI before the new rehearsal tests; no storage behavior change.
- Deviations/blockers: `main` advanced externally to `ac111cc256162fea9135707cd9e9a284e0894ff0` before PR merge. After explicit orchestrator authorization, merged that `main` into this branch without editing its workflows; the expanded focused and full canonical verification passed again.
