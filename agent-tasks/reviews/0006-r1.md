# Assignment 0006 review r1

- Assignment/protocol: 0006-stage17-runtime-entrypoint.md / common/v3.md.
- Implementation: added installed ah-there-it-is console entry point with a local-only serve subcommand, explicit host/port parsing, and a read-only runtime schema gate that requires an existing file-backed SQLite database whose Alembic head set exactly matches packaged migration heads before Uvicorn starts. Runtime serve never creates or migrates the database.
- Application/runtime scope: removed module-level app construction and module-level Jinja runtime object construction; create_app() remains the factory. Development just serve now uses Uvicorn factory mode with reload and explicit 127.0.0.1:8000. Installed serve uses factory mode without reload and reuses environment-backed Settings/provider configuration.
- Wheel/runtime proof: wheel metadata contains the console script; real wheel is installed outside the checkout, explicit packaged migration is run, missing/outdated databases are rejected before serving, and the installed console script serves /health, /, and packaged /static/style.css on localhost before graceful shutdown.
- Changed-file scope: pyproject console metadata; runtime CLI/schema gate; app factory side-effect cleanup; Just development serve recipe; focused runtime tests; extended real-wheel migration/runtime smoke; installed lifecycle documentation.
- Self-review: no schema migration, auto-migration, bootstrap/portable/backup semantics change, provider/prompt/network API change, dependency/runtime/GitHub Actions version change, service-manager/public-deployment work, or Stage 17 completion claim.
- Focused verification: 31 tests passed across runtime CLI/schema gate/import-side-effect, existing direct create_app behavior, and real-wheel installed HTTP smoke.
- Canonical verification: just check PASS (210 tests), just migration-check PASS, just corpus-check PASS, just scenario-check PASS, just scenario-eval PASS (42/42), just provider-contract PASS (22 tests).
- Correction rounds before PR: 2 routine wheel-smoke harness corrections: made prefix installation independent of an already-installed editable package via --ignore-installed; changed test shutdown from raw SIGTERM assertion to Uvicorn-handled SIGINT graceful shutdown. Self-review also moved Jinja template construction inside create_app to keep module import runtime-state-light.
- Deviations/blockers: none.
