# Review: Assignment 0031 — lifecycle preflight and checkpoints

- Protocol: `agent-tasks/common/v7.md`.
- Batch: `post-0029-agent-execution-reliability-2026-09-24`.
- Control SHA: `faf4b6484180ba85f99d1147ac0026cc6350f827`.
- Seed SHA: `76c2dc2b1b8796e8a4af374a49488b386099f0b0`.
- Base main SHA / previous batch merge SHA: `a2f90ea076f07dd9e6d0a1b9348bac7f265793e4`.
- Implementation commit: `e33d587` (`feat(agent): add lifecycle preflight checkpoints`).

## Execution

- Host/account: local U24 shell as Linux user `gpt`.
- Execution channel: local commands, explicitly required by the user. Remote Commander and SSH were not used.
- No separate Codex CLI process was launched; implementation and verification ran directly in the authorized local shell.
- Structured controller observations are stored outside the worktree at `/home/gpt/.local/state/ah-there-it-is/agent-batch/post-0029/0031/controller-observations.jsonl`.

## Implementation and self-review

- Added `tools/agent/lifecycle_checkpoints.py`, a stdlib-only helper outside application runtime. Its preflight reads fetched local refs and manifest/spec blobs at the immutable control SHA; it does not fetch or mutate Git state.
- Preflight reports repository path/remote, clean status, branch, local `main` versus `origin/main`, optional expected start SHA, fetched control ref/SHA, manifest task order, selected task fields, and target task branch existence.
- Seed verification checks the task branch, clean worktree, seed HEAD or later ancestry, one ordinary commit above the expected base, parent SHA, and byte-identical assignment contents from the control blob.
- Checkpoints live outside the repository. Writes use a same-directory temporary file, file and directory fsync, and atomic replacement. Reload validates finite phases, monotonic history, correction iteration records, PR/merge fields, and stale repository branch/HEAD state. A correction keeps the highest phase and full history; status does not decide whether work should be rerun.
- Added temporary local Git repository tests for start SHA, dirty worktree, control SHA, exact assignment bytes, seed ancestry, checkpoint monotonicity/corrections, atomic writes, PR/merge lifecycle, and malformed/stale state.
- Changes are limited to agent tooling, its tests, and factual documentation. No application behavior, dependency, provider, CI, or inventory semantic changes.

## Verification

- Focused: `python -m pytest tests/test_lifecycle_checkpoints.py` — 12 passed.
- Real preflight against the local clean `main` worktree — all checks passed; `main` and fetched `origin/main` were `a2f90ea076f07dd9e6d0a1b9348bac7f265793e4`.
- Real seed verification in a clean local audit checkout — verified branch `feat/agent-lifecycle-checkpoints`, seed parent/base, one-commit seed, and exact assignment bytes.
- Final canonical set:
  - `just check` — 384 passed; one pre-existing Starlette deprecation warning.
  - `just migration-check` — passed; no new upgrade operations.
  - `just corpus-check` — 58 cases loaded.
  - `just scenario-check` — 58 scenarios loaded.
  - `just scenario-eval` — 58/58 completed; all 58 postcondition sets passed; none unchecked.
  - `just retrieval-eval` — 86/86 passed; candidate-starvation diagnostic passed.
  - `just provider-contract` — 23 passed.
- No live provider/model calls.
- PR and hosted CI were pending at review creation.

## Deviation

- The user explicitly required local commands as `gpt` and prohibited Remote Commander and other channels. This superseded v7 launcher channel selection for this session. Immutable seed, exact assignment, canonical verification, one PR, bounded CI, merge commit, clean-main synchronization, and seed ancestry checks remain in force.
- No other deviations or blockers at review creation.
