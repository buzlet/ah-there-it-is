<!-- 0009-r1.md -->
# Assignment 0009 review record (r1)

Protocol: `agent-tasks/common/v3.md`. Branch: `feat/stage20-database-doctor`.

## Implementation and scope

- Added a typed, provider-independent doctor report for the configured, existing, exact-current-head SQLite database. Checks cover integrity, foreign keys, normalized names and sibling identity, hierarchy cycles, item state/quantity, and FTS schema/content. Supported duplicate item identities are warnings.
- Added installed `doctor` JSON/exit-code command and separate explicit `repair-search-index`. Repair rechecks base invariants under its write transaction, replaces only derived FTS objects/content, and reruns doctor.
- Shared the bounded FTS consistency function with portable import. Added deterministic corruption, read-only, repair, 1000-item, WAL, portable-import, and installed-wheel coverage.
- Changed files are limited to the doctor/FTS boundary, runtime CLI, portable-import consistency call, tests, this record, and the relevant README section. No migration, dependency, workflow, provider, or startup behavior changed.

## Self-review

- Reviewed the branch diff against Assignment 0009 and runtime/recovery contracts. `validate_database`, bootstrap, restore, migrations, and serve semantics remain unchanged.
- Doctor opens SQLite read-only; when no WAL is present it uses an immutable read to avoid creating WAL sidecars. With an active WAL it reads committed WAL content without checkpointing or changing the database/WAL files. Missing WAL shared memory is reported instead of created.
- FTS samples are capped at 20 IDs while counts remain complete. Repair refuses missing/outdated databases and all non-FTS failures; authoritative/base-table snapshots remain identical after repair and repeat repair.
- Diff whitespace check is clean. No known scope creep or blocker.

## Verification

- Focused: doctor corruption/read-only/repair/scale tests and portable, storage, runtime, and installed-wheel tests passed. The installed wheel reports missing and healthy databases through the real console entry point.
- Canonical final run: `just check` 247 passed (one existing dependency deprecation warning); `just migration-check` clean; `just corpus-check` 42 cases; `just scenario-check` 42 scenarios; `just scenario-eval` 42 completed with 42 checks passed; `just provider-contract` 22 passed.
- Pre-PR correction rounds: 2. Corrected SQLite sidecar creation in read-only diagnosis and closed SQLite handles deterministically. Final focused and complete canonical checks passed after corrections.
- Deviations/blockers: none.
