<!-- 0010-r1.md -->
# Assignment 0010 review record (r1)

Protocol: `agent-tasks/common/v3.md`. Branch: `feat/stage21-manual-web-admin`.

## Implementation and scope

- Added `InventoryService.update_category` and `update_location` with omitted-versus-null support, pre-mutation sibling and cycle validation, stable IDs, and no synthetic Item history.
- Added strict Item/Category/Location create and edit API schemas, stable response DTOs, and browser forms. Manual Item create/edit runs through InventoryService with explicit provenance; combined field/location edits now commit or roll back as one HTTP transaction.
- Added full Item form fields, explicit category/location root choices, edit pages with full-path parent options excluding descendants, and deterministic vanilla-JavaScript submission. Existing `/items` pagination remains unchanged.
- Changed files are limited to inventory service, web routes/schemas/templates/static assets, focused tests and wheel smoke, README, and this review record. No delete endpoint, migration, provider, runtime, dependency, or workflow change.

## Self-review

- Reviewed the complete branch diff against Assignment 0010 and the existing agent/service transaction boundary. All browser inventory writes call InventoryService; no route assigns ORM inventory fields directly.
- Tree changes validate names, references, sibling identity, and ancestor chain before mutation. Stable Item/Event location and category references remain untouched; paths come from authoritative rows.
- Item edits can clear nullable/list/object fields; existing intentional duplicates remain editable without identity change. No-op edits add no history event, and a failed move rolls back preceding field edits.
- New templates and `tree.js` are packaged and served from the installed wheel. Diff whitespace check is clean; no known scope creep or blocker.

## Verification

- Focused: service tree invariants, full manual Item create/edit/search/history, HTTP 400/404/422 and atomicity, HTML forms/parent choices, target-scale pagination, installed-wheel assets, and JavaScript syntax checks passed.
- Canonical final run: `just check` 254 passed (one existing dependency deprecation warning); `just migration-check` clean; `just corpus-check` 42 cases; `just scenario-check` 42 scenarios; `just scenario-eval` 42 completed and 42 checks passed; `just provider-contract` 22 passed.
- Pre-PR correction rounds: 0.
- Deviations/blockers: none.
