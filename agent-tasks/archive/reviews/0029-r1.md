<!-- 0029-r1.md -->
# Assignment 0029 review — Windows Git Bash implementation-host enablement

- Protocol: v5-batch wrapping v4 for this U24 execution; deliverable protocol v6.
- Batch: `windows-git-bash-enablement-2026-09-24`.
- Immutable control SHA: `5f60799b943619b7e89c31b2f175aa151601e2f2`.
- Previous batch merge / batch-start main / `base_main_sha`: `f5a05fa4b723ed1bca37b8137f445594f58fdebd`.
- `seed_sha`: `285f691bf337d2bb9266b69c282985ebd3607873`.
- Branch: `feat/windows-git-bash-implementation-host`.

## Implementation and scope

- Added v6 with launcher-selected `u24-bash` and `windows-git-bash`, deterministic per-command preludes, native Windows Python preflight, preserved seed/batch/PR/merge controls, default canonical recipes and bounded CI waiting.
- Made Just select Bash explicitly; moved migration verification to an OS temporary directory and retrieval reports to an OS temporary file. Default scenario evaluation prints JSON without writing the checkout.
- Reworked installed-wheel smoke around a temporary venv, offline local wheel install, platform-correct interpreter/console entry point, and installed-package path verification. Preserved Stage 26 packaged template/static, migration, CLI, browser, backup and runtime assertions.
- Updated existing Linux CI job to call evaluation recipes with defaults while retaining uploaded JSON reports; added minimal LF rules and factual README/AGENTS/HANDOFF status.
- Changed files are limited to protocol/status docs, Justfile, verification helpers, wheel/data-path/evaluation tests, `.gitattributes`, and the existing CI workflow. No domain/search change, dependency upgrade, Windows CI matrix, or PowerShell/cmd implementation path.

## Self-review and local verification

- Compared `seed_sha..HEAD` scope with the issued spec. Historical v4/v5, immutable manifest and assignment bytes remain unchanged. Audited touched paths for `/tmp`, Unix `bin`/`site-packages` assumptions, sticky activation, recipe arguments, unbounded watchers, and CI duplication.
- Focused: installed-wheel, data-path and retrieval tests pass (17 tests); actual migration and retrieval recipes pass; CI-style scenario and retrieval report collection produces valid JSON without a local worktree artifact.
- Canonical U24 run: `just check` (361 passed), `just migration-check`, `just corpus-check` (58), `just scenario-check` (58), `just scenario-eval` (58 completed, 58 checks passed), `just retrieval-eval` (86/86 and diagnostic passed), `just provider-contract` (23 passed). Default recipes only; clean of generated tracked/untracked reports.
- Before-PR correction rounds: 2. Removed inherited `PYTHONPATH` from the temporary-venv pip install after focused wheel smoke exposed a false already-installed package; selected Windows `CTRL_C_EVENT` during self-review because Uvicorn handles SIGINT, leaving actual process behavior to native pilot.
- CI correction rounds: 0 pending PR checks.

## Native observation boundary

0029 ran on U24 only. The first native Windows pilot must run v6 preflight and full canonical suite, then observe `LOCALAPPDATA`/home and installed CLI paths, backup/restore, SQLite WAL/checkpoint and byte integrity, file replacement/locking, and process startup/cleanup. Do not infer Windows correctness from simulated pure path tests or add speculative skips/xfails.
