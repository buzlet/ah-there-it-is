# Assignment 0007 review r1

- Assignment/protocol: 0007-stage18-stable-data-home.md / common/v3.md.
- Implementation: added a stdlib-only side-effect-free per-user data-path resolver; default Settings/get_settings database URL now resolves to data-home/inventory.db unless a non-blank AH_THERE_IT_IS_DATABASE_URL is explicitly configured. AH_THERE_IT_IS_DATA_DIR overrides only the default data home.
- Platform policy: Unix uses XDG_DATA_HOME/ah-there-it-is or ~/.local/share/ah-there-it-is; macOS uses ~/Library/Application Support/AhThereItIs; Windows uses LOCALAPPDATA/AhThereItIs with home/AppData/Local/AhThereItIs fallback. Relative override/base paths are anchored to home rather than CWD.
- Runtime/storage scope: added read-only installed paths JSON surface; explicit non-file SQLite configuration is reported safely without echoing credentials. Settings resolution, paths, create_app, app import, and serve schema gate create no data directory/database. Only explicit storage_cli upgrade creates a missing file-backed SQLite parent before Alembic writes.
- Wheel proof: real installed wheel resolves the same default DB from two unrelated CWDs under controlled XDG/HOME, paths creates nothing, explicit upgrade from one CWD creates the current-schema stable DB, installed serve from the other CWD serves it without AH_THERE_IT_IS_DATABASE_URL, and neither CWD gets stray database/WAL/SHM files.
- Changed-file scope: data-path resolver; config precedence/default URL; runtime paths CLI; explicit upgrade parent creation; focused path/runtime tests; extended wheel cross-CWD smoke; installed lifecycle documentation.
- Self-review: no schema migration, bootstrap/portable/backup behavior change, prompt/provider/model/scenario change, dependency/runtime/GitHub Actions change, service-manager/installer work, or Stage 18 completion claim. mkdir appears only in explicit upgrade code.
- Focused verification: 46 tests passed across platform path selection, blank/override precedence, side-effect-free settings/create_app/paths/serve gate, explicit upgrade parent creation, runtime/app behavior, and real-wheel cross-CWD serve.
- Canonical verification: just check PASS (225 tests), just migration-check PASS, just corpus-check PASS, just scenario-check PASS, just scenario-eval PASS (42/42), just provider-contract PASS (22 tests).
- Correction rounds before PR: 0 implementation/test correction rounds. Tool-call transport retries caused by command-wrapper quoting changed no repository state.
- Deviations/blockers: none.
