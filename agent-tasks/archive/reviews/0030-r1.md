# Review: Assignment 0030 — durable Codex session runner

- Protocol: `agent-tasks/common/v7.md`.
- Batch: `post-0029-agent-execution-reliability-2026-09-24`.
- Control SHA: `faf4b6484180ba85f99d1147ac0026cc6350f827`.
- Seed SHA: `457359513073ae441507e189c93ab912bb35b704`.
- Base main SHA / previous batch merge SHA: `25453f291a3c0a8d3a5c77a6ef8adfc533226c62`.
- Local implementation commit: `6c1f3d8894eda71d541b955a9b215bc86a5fe9a9`.

## Execution

- Host/account: local U24 shell as Linux user `gpt`.
- Execution channel: local commands, explicitly required by the user because other channels were unavailable. Remote Commander and SSH were not used.
- No live Codex CLI was launched for implementation; Codex CLI was not on the `gpt` PATH. Tests use fake executable processes only.
- SSH interruption: no SSH session used.
- Session resume: none.
- Controller timeout/hang diagnostics: none.

## Implementation and self-review

- Added a stdlib-only POSIX runner outside the application package. It launches the exact `exec --json --full-auto -` argument sequence without a shell, sends the prompt bytes through stdin, and stores only the prompt SHA-256.
- Each unique run ID gets a private directory outside the repository worktree with metadata, stdout JSONL, stderr, and an atomic terminal result. Duplicate run IDs are refused.
- Status checks process-group membership and Linux process start ticks, so a PID number alone does not establish that a run is active.
- Tail reads have byte and line bounds. Termination sends SIGTERM to the named process group first, then uses a bounded SIGKILL escalation and records the outcome.
- Seed contents are exact copies of the 0030 specification and immutable batch manifest; the v7 protocol already existed in base with a blob identical to the control SHA. Implementation changes are limited to `tools/agent/**` and `tests/test_codex_session.py`; factual status/review docs are `AGENTS.md` and this record. No application, dependency, provider, or inventory-semantic changes.
- The helper does not persist environment variables, authentication files, tokens, the prompt body, or SSH material. It does not automate login.
- Raw `codex exec --json --full-auto -` remains a documented protocol fallback.

## Verification

- Focused: `python -m py_compile tools/agent/codex_session.py tests/test_codex_session.py` passed.
- Focused: `python -m pytest tests/test_codex_session.py -q` — 11 passed.
- Final canonical set:
  - `just check` — 372 passed; one pre-existing Starlette deprecation warning.
  - `just migration-check` — passed; no new upgrade operations.
  - `just corpus-check` — 58 cases loaded.
  - `just scenario-check` — 58 scenarios loaded.
  - `just scenario-eval` — 58/58 completed, all 58 postcondition sets passed, none unchecked.
  - `just retrieval-eval` — 86/86 passed; candidate-starvation diagnostic passed.
  - `just provider-contract` — 23 passed.
- No live provider/model calls.
- Pre-PR correction iterations: 1. The first focused run exposed a test timing race between the atomic result file and the immediately following metadata status update; the test now waits for the durable metadata update.
- Post-CI correction iterations: 0 at review creation; PR/CI pending.

## Deviation

- User-directed local execution superseded v7's launcher channel selection for this session. All non-channel lifecycle controls remain in force: immutable seed, exact assignment bytes, focused and canonical verification, one implementation PR, bounded CI, merge commit, clean-main synchronization, and seed ancestry proof.
- No other deviations or blockers at review creation.
