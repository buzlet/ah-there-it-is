<!-- agent-tasks/reviews/0022-r1.md -->
# Review: Assignment 0022 — explicit non-loopback serve guard

- Protocol: v5 batch wrapper with the v4 per-assignment lifecycle.
- Batch ID: `post-0018-evidence-usability-2026-09-24`.
- Control SHA: `43c00657d8a87f49bfb81989d490ff16c2c58797`.
- Previous batch merge / base main SHA: `d02aef613efb59139abed2cc986a6cd70c6db1dd`.
- Branch: `feat/hardening-nonlocal-serve-guard`.
- Seed SHA: `ee127ebdc666d44ab9b394568b12f8f91dac1caa`.

## Implementation and self-review

- Installed `serve` retains host `127.0.0.1`; non-loopback hosts are rejected before schema validation and Uvicorn unless `--allow-nonlocal` is present.
- Classification allows IPv4 `127.0.0.0/8`, IPv6 `::1`, and case-insensitive `localhost`. Opted-in non-loopback starts warn on stderr that the application has no authentication.
- Added runtime classification/order/opt-in tests and installed-wheel CLI coverage. `just serve`, schema gating and data creation behavior are unchanged; no environment bypass or schema/dependency/provider changes.
- Changed files: `runtime_cli.py`, runtime CLI tests, installed-wheel tests, `AGENTS.md`, this review.

## Verification

- Focused: `PYTHONPATH=src python -m pytest tests/test_runtime_cli.py tests/test_wheel_migrations.py` passed (35 tests).
- Canonical: `just check` passed (339 tests); migration-check passed; corpus-check and scenario-check passed (42 cases each); scenario-eval completed 42/42 with 98 state checks and 0 failures; provider-contract passed (23 tests).

## Corrections and outcome

- No code corrections before PR. Initial focused invocation omitted `PYTHONPATH=src` and resolved a stale editable package from the neighboring checkout; corrected invocation passed without code changes. CI correction iterations: 0 at initial PR head.
- Deviations/blockers: none.
