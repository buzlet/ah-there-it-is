<!-- agent-tasks/reviews/0024-r1.md -->
# Review: Assignment 0024 — bounded FTS candidate-starvation fix

- Protocol: `agent-tasks/common/v5-batch.md` with the v4 per-assignment lifecycle.
- Batch ID: `post-0023-stage26-2026-09-24`.
- Control SHA: `29bfba2e372207c8f71b6cbe783fa388689cca0f`.
- Previous batch merge / batch-start base main SHA: `789dea44c726935de76e66ca8d1f22a911756431`.
- Branch: `feat/search-fts-candidate-starvation`.
- Seed SHA: `d1f896bc0ec09e5e78b0e3bc51604efbadc61a18`.
- Base main SHA: `789dea44c726935de76e66ca8d1f22a911756431`.
- Implementation SHA: `dd2d22d6ce3fc86127c9e0bf7a706a20eafbf228`.

## Implementation and self-review

- `SearchService.search_items` now scans `min(max(candidate_limit, 100), 500)` FTS rows, preserving the existing candidate budget for larger requests up to the hard cap. Exact/name/alias/tag/attribute acquisition, match types, scores and final ordering are unchanged.
- Kept FTS acquisition to one bounded query. `_fts_overlap_ok` semantics remain unchanged.
- Promoted `candidate-starvation-blue-box` to a gating diagnostic. The report records the effective bound, target FTS rank and top-N result; the CLI exits non-zero if the diagnostic fails. Its rank-probe query is also capped at 500 rows.
- The measured target remains FTS rank 36, is within the ordinary scan budget of 100, and appears in the requested top five.
- No schema, migration, dependency, provider, workflow or non-FTS candidate policy changes.
- Changed files: `services/search.py`, `retrieval_eval.py`, their focused tests, and `agent-tasks/designs/post-0023-decisions.md`.

## Verification

- Focused: `tests/test_search.py`, `tests/test_retrieval_eval.py`, and `tests/test_target_scale.py` passed. `just retrieval-eval` passed 86/86 and the starvation diagnostic.
- Canonical: `just check` passed (344 tests; one existing Starlette deprecation warning); `just migration-check` passed with no new upgrade operations; corpus-check and scenario-check each validated 42 cases; scenario-eval passed; provider-contract passed (23 tests); retrieval-eval passed 86/86 with the diagnostic passed.
- FTS bounds test verifies budgets 100 for limit 5, 400 for limit 100, and the hard cap 500 for limit 126.

## Corrections and outcome

- Pre-PR implementation/test failure corrections: 0. One hard-cap refinement and clearer larger-limit assertion were completed before final verification; the final focused and canonical runs passed.
- CI correction iterations: 0 at review creation; no PR checks have run yet.
- Verification invocation retries: corrected the venv/PYTHONPATH selection and the positional `just scenario-eval` arguments; no test or implementation failure remained.
- Deviations/blockers: none.
