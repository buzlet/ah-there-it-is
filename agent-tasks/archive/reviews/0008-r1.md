# Assignment 0008 review r1

- Assignment/protocol: 0008-stage19-target-scale-read-paths.md / common/v3.md.
- Implementation: item search now discovers a bounded SQL/FTS candidate pool (exact name/alias/tag/attribute, normalized contains, FTS) and applies the existing Python ranking only to candidate Items; aliases/tags are eager-loaded only for that bounded pool and result context is loaded only for final candidates.
- Location suggestions: related evidence is selected only from same-category/shared-tag rows using column projections plus correlated tag membership, rather than hydrating every located Item. Stage 15 evidence/ranking/read-only semantics are preserved.
- Catalog/web: added ItemPage with default 50 / hard max 100, deterministic normalized-name+ID ordering, total/page metadata and previous/next links. /items uses the paged projection. Location/category direct item counts use grouped SQL and loaded parent-id maps, eliminating per-node item-count lazy loads.
- Schema: exactly one new revision b62f9d8a3c41 adds ix_item_tags_tag_id; ItemTag model metadata matches it; CURRENT_SCHEMA_REVISION and wheel head expectation advance to the new head. Upgrade/downgrade/schema-check round trip is covered.
- Target-scale fixture: deterministic test-only generator creates exactly 1000 items, 200 nested locations, 31-category representative tree, aliases/tags/attributes/descriptions, duplicate leaf locations, unknown-location items, and named search/suggestion targets in one InventoryService transaction.
- Structural scale proof: representative exact name/alias/attribute/tag/FTS search semantics pass while limit-5 Item ORM loading stays below 200 of 1000; suggestion Item ORM loading stays below 20; /items page 1 is 50 of total 1000 and later slices are deterministic; location/category lists remain <=3 SQL statements independent of node count.
- Changed-file scope: search/suggestion/catalog services; /items route/template; ItemTag index model/migration/current-head metadata; deterministic scale fixture/tests; migration/wheel head assertions; minimal Stage 19 documentation.
- Self-review: no bootstrap/portable/backup/history/provider/prompt/scenario behavior change, no dependency/runtime/GitHub Actions change, no timing gates, no external search/cache/ORM replacement, and no Stage 19 completion claim.
- Focused verification: 49 tests PASS across existing search/suggestion semantics, target-scale structural coverage, migration round trip/check, web behavior, and real-wheel smoke.
- Canonical verification: just check PASS (230 tests), just migration-check PASS, just corpus-check PASS, just scenario-check PASS, just scenario-eval PASS (42/42), just provider-contract PASS (22 tests).
- Correction rounds before PR: 2 routine focused rounds: fixed grouped-result materialization and isolated the suggestion target category in the scale fixture; then corrected the expected category-node count from 30 to 31 after adding that dedicated fixture category.
- Deviations/blockers: none.
