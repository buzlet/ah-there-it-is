# 0090 — Final integrated MVP candidate review

## Objective

Close the implementation batch as an **MVP candidate for independent review**, not as a self-approved release.

No new feature work belongs here.

## Required work

- Cumulatively review seed..HEAD against accepted product/design decisions.
- Recheck that no new product scope entered through hardening fixes.
- Confirm Web/Agent/Telegram share the intended mutation/idempotency boundaries.
- Confirm quantity/lifecycle/Undo/media semantics remain coherent.
- Confirm evaluation hard gates remain fail-closed after unrelated fixes.
- Confirm no image bytes/vision, Telegram groups/webhooks/photos, multi-user, QR/barcode, voice, multilingual search, Item merge or generic hard-delete appeared.
- Review every `DIRECT FOLLOW-UP` generated by 0084–0089. Do not implement host-specific work here.
- Create/update the batch self-review at:
  `agent-tasks/reviews/0084-0090-final-mvp-hardening-r1.md`
  with:
  - concrete findings reproduced;
  - regression tests added;
  - corrections and checkpoint SHAs;
  - areas independently rechecked and found correct;
  - exact Direct follow-ups;
  - remaining risks.

Do not declare production/deployment readiness.

## Final focused integration

Run these groups separately so a sandbox command timeout does not hide results.

### Group A — mutation/chat/Telegram

    .venv/bin/python -m pytest -q       tests/test_agent.py       tests/test_app.py       tests/test_chat_application_service.py       tests/test_atomic_turn_receipts.py       tests/test_write_target_safety.py       tests/test_idempotency.py       tests/test_idempotency_crash.py       tests/test_quantity_semantics.py       tests/test_quantity_split.py       tests/test_removed_lifecycle.py       tests/test_undo.py       tests/test_item_media.py       tests/test_media_telegram_integration.py       tests/test_telegram_adapter.py       tests/test_telegram_client.py       tests/test_telegram_runtime.py

### Group B — persistence/package/boundedness

    .venv/bin/python -m pytest -q       tests/test_target_scale.py       tests/test_database_doctor.py       tests/test_storage.py       tests/test_portable_v3.py       tests/test_portable_compatibility.py       tests/test_portable_streaming_import.py       tests/test_migrations.py       tests/test_wheel_migrations.py       tests/test_runtime_cli.py       tests/test_trace_config_privacy.py

### Group C — evaluation subsystem

    .venv/bin/python -m pytest -q       tests/test_benchmark_campaign.py       tests/test_benchmark_comparison.py       tests/test_promotion_report.py       tests/test_live_eval.py       tests/test_live_compare.py       tests/test_model_probe.py       tests/test_evaluation.py       tests/test_evaluation_export.py       tests/test_scenario_eval.py       tests/test_eval_checks.py

Then:

    make provider-contract
    make migration-check
    make corpus-check
    make scenario-check
    make scenario-eval
    make retrieval-eval
    make compile
    git diff --check

Do not run a repository-wide local `make check`; `full_local_required: false`.
The authoritative exact-head Python 3.12 application CI owns the full-suite gate.

## Lifecycle

Open one PR and obtain green exact-head CI.

**Do not merge.**

Stop at:

`READY FOR INDEPENDENT REVIEW`

and return the PR number, exact head, CI run, seed ancestry, self-review path and DIRECT FOLLOW-UP list.
