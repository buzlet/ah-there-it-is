# Implementation task workspace

Default agent reading:

1. root `AGENTS.md`;
2. exact active immutable batch manifest/task specs;
3. `agent-tasks/common/v8.md`;
4. relevant source/tests.

Do not recursively scan this directory.

Active layout:

- `common/v8.md` — only active protocol;
- `assignments/` — current issued material only;
- `reviews/` — one current integrated-batch review;
- `batches/` — current batch control/result material;
- `designs/` — unresolved/current decisions;
- `archive/` — historical evidence, not current instructions.

Completed material is archived after acceptance.
