# Active protocol

Current protocol: `v7.md`.

Historical generations `v1`–`v6` and the superseded first v7 SSH/Codex transport draft are in:

`agent-tasks/archive/common/`

Do not read historical protocols during ordinary implementation work unless explicitly instructed.
